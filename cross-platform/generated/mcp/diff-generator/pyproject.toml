[project]
name = "cortex-mcp-diff-generator"
version = "1.3.0"
description = "Generates structured diffs between code versions, documents, or configurations with semantic understanding of changes."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
