[project]
name = "cortex-mcp-skill-validator"
version = "1.3.0"
description = "Validates MCP skill definitions, schemas, and implementations against specification requirements and quality standards."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
