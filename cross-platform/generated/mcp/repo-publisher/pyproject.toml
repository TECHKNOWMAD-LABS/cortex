[project]
name = "cortex-mcp-repo-publisher"
version = "1.3.0"
description = "Automates repository publishing workflows including versioning, changelog generation, and package registry distribution."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
