[project]
name = "cortex-mcp-agent-output-validator"
version = "1.3.0"
description = "Validates and scores agent outputs against expected schemas, quality thresholds, and correctness criteria for reliable autonomous pipelines."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
