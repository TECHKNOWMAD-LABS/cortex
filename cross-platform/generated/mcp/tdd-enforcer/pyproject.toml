[project]
name = "cortex-mcp-tdd-enforcer"
version = "1.3.0"
description = "Enforces test-driven development practices by requiring tests before implementation and validating coverage thresholds."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
