[project]
name = "cortex-mcp-scenario-simulator"
version = "1.3.0"
description = "Simulates complex scenarios and what-if analyses to evaluate outcomes, risks, and decision paths before execution."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
