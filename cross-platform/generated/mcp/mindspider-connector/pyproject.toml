[project]
name = "cortex-mcp-mindspider-connector"
version = "1.3.0"
description = "Connects to and orchestrates MindSpider web crawling and scraping operations for structured data extraction."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
