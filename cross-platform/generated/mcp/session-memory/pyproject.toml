[project]
name = "cortex-mcp-session-memory"
version = "1.3.0"
description = "Manages within-session memory and state tracking for maintaining coherent context across multi-turn agent interactions."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
