[project]
name = "cortex-mcp-persistent-memory"
version = "1.3.0"
description = "Provides long-term memory storage, retrieval, and semantic search across agent sessions for persistent knowledge retention."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
