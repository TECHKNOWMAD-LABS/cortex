[project]
name = "cortex-mcp-pre-package-pipeline"
version = "1.3.0"
description = "Runs pre-packaging validation, linting, testing, and build steps before skills are packaged for distribution."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
