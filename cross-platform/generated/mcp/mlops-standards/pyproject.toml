[project]
name = "cortex-mcp-mlops-standards"
version = "1.3.0"
description = "Enforces MLOps best practices, standards compliance, and operational readiness checks for ML model deployments."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
