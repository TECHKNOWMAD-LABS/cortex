[project]
name = "cortex-mcp-dev-lifecycle-engine"
version = "1.3.0"
description = "Manages the full software development lifecycle from planning through deployment with automated stage transitions and quality gates."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
