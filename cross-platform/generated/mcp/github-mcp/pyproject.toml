[project]
name = "cortex-mcp-github-mcp"
version = "1.3.0"
description = "Interfaces with GitHub APIs for repository management, issue tracking, pull requests, and CI/CD workflow automation."
requires-python = ">=3.12"
dependencies = ["fastmcp~=3.1.0", "python-dotenv~=1.0.0"]
