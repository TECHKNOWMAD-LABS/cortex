# Rubric for tdd-enforcer
# Used by eval_judge.py to customize evaluation weights

skill_context: "Focus on test quality metrics and anti-pattern detection accuracy."

# Weight overrides (must sum to ~1.0)
weights:
  correctness: 0.25
  reasoning_depth: 0.20
  completeness: 0.25
  hallucination_risk: 0.20
  coherence: 0.10

# Score interpretation
passing_threshold: 0.65
excellence_threshold: 0.85
